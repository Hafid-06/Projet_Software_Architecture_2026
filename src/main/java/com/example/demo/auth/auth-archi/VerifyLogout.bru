meta {
  name: VerifyLogout
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/auth/verify?token={{token}}
  body: none
  auth: inherit
}

params:query {
  token: {{token}}
}

settings {
  encodeUrl: true
}
