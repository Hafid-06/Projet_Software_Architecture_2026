meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url:  {{baseUrl}}/auth/logout?token={{token}}
  body: none
  auth: inherit
}

params:query {
  token: {{token}}
}

settings {
  encodeUrl: true
}
