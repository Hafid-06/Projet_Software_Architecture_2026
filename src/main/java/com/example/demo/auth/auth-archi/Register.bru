meta {
  name: Register
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/register
  body: json
  auth: inherit
}

body:json {
  {"email":"auth@example.com","credential":"pass123"}
}

settings {
  encodeUrl: true
  timeout: 0
}
