meta {
  name: Modifie
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/credentials/{{credentialId}}
  body: json
  auth: inherit
}

body:json {
  { "identityId": 1, "authorityId": 2, "value": "secret" }
}

settings {
  encodeUrl: true
  timeout: 0
}
